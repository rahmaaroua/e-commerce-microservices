server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git: { uri: https://github.com/rahmaaroua/e-commerce-microservices, clone-on-start: true }
    compatibility-verifier:
      enabled: false


